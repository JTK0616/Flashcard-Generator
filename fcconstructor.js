// constructor function for creating new flashcard objects

var BasicCard = function(front, back) {
  this.front = front;
  this.back = back;
};

// exporting the BasicCard constructor.  Required in basiccard.js

module.exports = var BasicCard;